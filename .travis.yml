language: python
python:
  - "3.6"
  - "3.6-dev"  # 3.6 development branch
  - "3.7-dev"  # 3.7 development branch
# command to install dependencies
install:
  - 'pip install pipenv'
  - 'pipenv sync'
  - 'pipenv install'
# to start the test
script:
  - python -m pytest
  - black --check --verbose
branches:
  only:
  - gh-pages
  - /.*/
